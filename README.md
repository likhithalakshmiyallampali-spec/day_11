# day_11
The Blog API is a backend application built with Node.js and Express.js. It provides a robust system for managing blog posts through a RESTful interface. The project emphasizes clean code architecture, data persistence using a local JSON file, and strict input validation to ensure data integrity. 
